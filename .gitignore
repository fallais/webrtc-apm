/bin/
*.test
*.out
.DS_Store

# Source-built native deps from `make deps`.
/.deps/

# Compiled binaries written to the repo root by `go build .` shortcuts.
/pion
/pion-aec

# WAV files (none of the examples write them anymore, but ad-hoc test
# runs may produce some).
*.wav
/runs/
